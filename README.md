# AiChat

> 📖 **English Version** — [README_FILES/README_EN.md](README_FILES/README_EN.md)

一款**角色扮演类微信聊天风格**的 Flutter Android 应用：内置多个角色，可与角色进行一对一的沉浸式聊天（支持发送图片、合并转发、角色包导入导出、自动更新等）。

> 纯 Cupertino（iOS 风格）UI，仅适配 Android。

---

## 声明

- 本应用开发者反对任何违法违规的AI内容生成，任何生成内容后果请自行承担。
- 本应用承诺所有代码全部开源，不收集任何用户数据。
- **用户数据完全自主**：所有数据（聊天记录、角色包、朋友圈、记忆点等）均可自行导入/导出、查看/分享，用户拥有完全的数据控制权。
- 本应用不提供任何充值付费服务，请注意辨识。
- 本应用仅供学习交流使用，请勿用于商业用途。

---

## 目录

### 用户指南

- [功能特性](#功能特性)
- [语C模式（自定义演绎）](#语c模式自定义演绎)
- [应用预览](#应用预览)
- [如何添加角色（以 CharactersImport 为例）](#如何添加角色以-charactersimport-为例)
- [朋友圈数据包（导入 / 导出）](#朋友圈数据包导入--导出)
- [持久化记忆（保存与导入 / 导出）](#持久化记忆保存与导入--导出)
- [群聊功能](#群聊功能)
- [创意工坊](#创意工坊)
- [自行配置对象储存（COS / OSS）](#自行配置对象储存cos--oss)
- [创意工坊表情包 ZIP 打包标准](#创意工坊表情包-zip-打包标准)
- [自动发朋友圈与主动问候](#自动发朋友圈与主动问候)
- [聊天背景与 UI 样式](#聊天背景与-ui-样式)
- [Token 消耗统计](#token-消耗统计)
- [记忆池（跨场景记忆）](#记忆池跨场景记忆)
- [朋友圈可见性分组](#朋友圈可见性分组)
- [聊天记录导出 / 导入](#聊天记录导出--导入)
- [存储空间管理](#存储空间管理)
- [API 设置与压缩会话](#api-设置与压缩会话)

### 开发者指南

- [项目结构](#项目结构)
- [修改应用版本号（V1.0.0）](#修改应用版本号v100)
- [本地打包 APK](#本地打包-apk)
- [自动更新机制](#自动更新机制)
- [技术栈](#技术栈)

---

## 功能特性

### 核心聊天
- **沉浸式角色聊天**：内置多个角色（人设、说话风格、核心记忆），支持自定义人设与用户关系
- **语C模式**：可在短信模式与括号动作流语C之间切换，支持自定义剧情推进风格、剧情行动、流式回复和候选行动
- **合并转发**：多选消息合并转发为卡片，独立展示并显示我方头像
- **图片消息**：通过「API 设置」中添加的模型发送图片（需模型支持视觉）
- **Markdown 渲染**：AI 回复支持 Markdown 格式渲染（代码块、列表、表格等）
- **聊天气泡样式**：默认/崩铁/鸣潮/终末地四种气泡样式可选，支持自定义气泡颜色与字体颜色
- **聊天背景**：为每个会话设置独立的背景图片，支持高斯模糊度调节
- **聊天搜索**：在聊天记录中搜索关键词
- **联系人搜索**：在通讯录中搜索角色
- **消息引用**：长按消息可引用回复

### 群聊功能
- **多角色群聊**：创建包含多个角色的群聊，角色之间可相互对话
- **@ 角色**：在群聊中 @ 特定角色，被 @ 的角色必定发言
- **引用回复**：角色发言时可引用其他成员的消息进行回复
- **群头像**：支持自定义群头像，未设置时自动生成成员拼图头像
- **群简介**：设置群聊简介，角色会按简介语境进行对话
- **不说话概率**：配置角色在群聊中的沉默概率（0~1），默认 0.2
- **引用回复概率**：配置角色发言时引用其他消息的概率（0~1），默认 0.2
- **群聊上下文**：群聊可独立设置上下文条数，或跟随全局设置
- **未读消息**：群聊支持未读消息计数，离开群聊页面时累计新消息

### 角色包与创意工坊
- **角色包导入/导出**：`.zip` 角色包一键导入、导出，支持批量勾选
- **创意工坊**：从 GitHub/Gitee 仓库或 COS 类对象储存下载角色包、游戏包和表情包
  - 支持添加多个仓库，自动检测可用的 Release tag / 目录
  - 角色分类（V1.1.0）：包含 Profile.json 的角色文件夹 zip
  - 游戏分类（V1.0.0）：包含 moments.json 的朋友圈数据包
  - 表情包分类（V1.3.0）：导入 ZIP 内的 GIF / WebP / PNG / JPG/JPEG 表情包，图片文件名自动作为备注
  - 更新通知（V1.2.0）：Git 读 Release body；COS 读 `Note/*.md`（内容变化才提醒）
  - 支持 GitHub 下载代理加速
  - COS 对象储存：支持腾讯云 COS / 阿里云 OSS 等；可公有读，也可启用**访问密钥私有读**（密钥仅本机）
  - 列表懒加载：打开分类先拉首页约 500 条，搜索或「加载更多」再拉全量；缓存约 30 分钟
- **朋友圈数据包**：**【我】→ 管理当前朋友圈** 导入/导出角色的朋友圈数据包，支持勾选导出

### 记忆系统
- **持久化记忆**：长按聊天气泡批量保存为记忆点，随角色的系统提示词一起发送给模型（聊天与朋友圈互动均生效）；记忆随角色包导入 / 导出（写入 Profile.json）
- **记忆池（跨场景记忆）**：聚合角色在「当前会话之外」的近期记忆，让角色在私聊/群聊/朋友圈互动中保持跨场景的记忆连贯
  - 近期私聊内容
  - 近期朋友圈（最近 N 条贴文 + 全部点赞与评论）
  - 近期群聊内容（该角色参与的、最近有消息的群，每群最近 3 条文本消息）
  - 角色资料卡（昵称/关系/备注/签名/地区/性格/介绍/开场白/标签）
  - 用户资料（昵称/性别/地区/签名）
- **记忆池管理**：可按角色独立停用记忆池的各个来源

### 朋友圈
- **朋友圈互动**：用户发布朋友圈后，可见范围内的角色会自动点赞/评论
- **自动发朋友圈**：角色可按设定的周期自动生成并发布朋友圈内容
  - 可配置发布周期（小时）、周期内发布条数
  - 可设置朋友圈可见范围
  - 应用启动/回到前台时检查并补发布
- **朋友圈互动通知**：角色点赞/评论产生未读通知，朋友圈 tab 显示红点
- **朋友圈可见性分组**：自定义朋友圈可见范围分组
  - 仅自己可见
  - 全部角色可见（默认）
  - 自定义分组（选择特定角色）
- **朋友圈管理**：导入/导出朋友圈数据包

### 主动问候
- **主动问候**：角色在用户一段时间未聊天后，会主动发送问候消息
  - 可配置无消息多少小时后触发问候（默认 72 小时）
  - 应用启动/回到前台时检查并触发

### API 与模型
- **上下文管理**：设置携带上下文条数（1 ~ 999 条，或「无限制」），可精确输入
- **会话自动压缩**：上下文无限制时自动开启压缩，达到所选模型上下文阈值（可调）时自动把更早的历史消息压缩为摘要
- **模型思考强度**：聊天设置可选 关闭 / 低 / 默认 / 高 / 极高（映射 `reasoning_effort` / `enable_thinking`，是否生效取决于模型）
- **AI 思考内容**：接收模型思考过程但不回传；长按角色消息可「查看思考」；可选在气泡下方显示思考时长
- **厂商预设**：内置多个 AI 厂商配置预设（OpenAI、Google Gemini、小米 MiMo 等）
- **模型检测**：支持一键检测可用模型列表
- **Token 消耗统计**：输入 / 输出 / **其中思考** 分项累计（输出已含思考，对齐账单）；可一键重置

### UI 与个性化
- **明暗模式**：跟随系统 / 浅色 / 深色，可自定义主题色
- **气泡样式**：默认/崩铁/鸣潮/终末地四种气泡样式可选
- **UI 样式**：默认/终末地两种 UI 样式可选
- **气泡颜色自定义**：可分别设置自己/对方 × 浅色/深色的气泡颜色和字体颜色
- **头像框样式**：方形 / 仿 QQ 圆形两种头像框样式
- **启动图标**：自定义应用启动图标
- **聊天背景**：为每个会话设置独立的背景图片和模糊度

### 其他功能
- **自动更新**：启动时自动检测新版本（Gitee 优先、GitHub 备用双源），可分别自定义检测仓库；更新弹窗可选下载源，一键下载并安装 APK
- **聊天记录导出/导入**：导出为 zip（文本/图片/文件）；可选择是否包含 AI 思考过程
- **存储空间管理**：管理应用占用的存储空间（用户数据/软件缓存），支持清理
- **开发者日志**：开发者模式下可查看实时日志（朋友圈 AI 互动日志、软件通知、错误信息等）
- **系统通知**：角色在用户离开聊天界面时发来新消息，向系统通知栏推送通知（包含角色名称、消息内容与头像）
- **图片裁剪**：发送图片前支持裁剪
- **通讯录字母索引**：右侧 A–Z 快速定位，支持长按滑动连续跳转

### 数据自主权
- **角色包导入/导出**：一键导出角色包（`.zip`），包含角色资料、提示词、头像、朋友圈、记忆点等全部数据
- **聊天记录导出/导入**：聊天记录可导出为 zip 包（含文本、图片、文件），支持导入恢复
- **朋友圈数据包**：朋友圈内容可导出/导入，支持批量勾选
- **记忆点随角色包**：记忆点自动写入角色包，导入时自动恢复
- **存储空间管理**：查看并清理应用占用的存储空间

## 语C模式（自定义演绎）

语C模式用于脱离即时社交限制的角色扮演式对话。角色以括号动作流回复，例如：

```text
（抬眼看向你，指尖轻轻敲了敲桌面）你来了。
```

### 开启方式

进入 **聊天页面 → 右上角「...」→ 聊天设置 → 回复格式**，将「语C/短信模式」切换为「语C」。该设置为全局聊天设置，会同时影响私聊和群聊中的角色回复格式；切换回「短信」即可恢复原来的 JSON 短消息分条回复。

### 语C推进风格

语C模式支持三种推进风格，均不会替用户决定自己的行动、台词、心理或剧情结果：

- **自由**：自然延续当前场景与关系，可描写世界和其他角色的反应，保留用户角色的决定权；
- **剧情**：通过环境变化、NPC 行动、线索、阻力和合理后果推进世界，每次尽量留下可回应的互动切口；
- **陪伴**：以陪伴、关系互动和情绪体验为主，不主动引入高强度冲突、危险、狗血误会或强制转折。

### 输入与回复

- **普通消息**：直接输入台词或行动，角色会根据当前语C上下文继续演绎；
- **剧情行动**：点击输入框「+」面板中的 **剧情行动**，输入并添加一段由用户主导的行动或故事发展。该操作只写入剧情，不会自动触发角色回复，之后可再发送普通消息或点击回复按钮推进；
- **语C流式回复**：默认开启，角色正文会随着模型输出实时显示在同一条气泡中；关闭后改用普通请求，适配不支持 SSE 流式输出的接口；
- **篇幅**：每次回复约 1～2 组「（动作）+ 台词」，一两句即可，避免长段堆砌；
- **候选行动**：默认开启。每次语C回复后生成恰好 4 个可继续推进剧情的**具体台词或「（动作）台词」**（可直接填入输入框发送），禁止「关心对方」这类概括标签；候选项不写入聊天记录，也不替用户决定剧情结果；关闭「生成语C候选行动」后不再生成或显示。

### 语C上下文规则

- 语C回复使用角色的 `Prompt.txt`、双方关系、持久化记忆和当前会话历史；
- 不注入现实时间、角色活跃时段、朋友圈/群聊等场景外记忆池，避免现实社交规则干扰演绎；
- 角色不会擅自追加用户未写出的行动、台词、心理或决定；
- 语C正文保留完整的括号动作流，不会像短信模式一样按句子拆成多条消息，也不要求正文使用 JSON 或 Markdown；
- 语C模式仍支持在动作流中按需调用已导入的表情包，使用方式与短信模式一致。

### 相关设置

在 **聊天设置 → 回复格式 / 输入框** 中可调整：

| 设置 | 说明 |
| ---- | ---- |
| `语C/短信模式` | 在短信分条回复与括号动作流语C之间切换 |
| `语C推进风格` | 自由 / 剧情 / 陪伴；仅在语C模式下生效 |
| `生成语C候选行动` | 是否在语C回复后生成4条具体台词/行动 |
| `语C流式回复` | 是否使用 SSE 实时显示语C正文 |
| `模型思考强度` | 关闭 / 低 / 默认 / 高 / 极高 |
| `显示 AI 思考时长` | 角色气泡下方是否显示「思考 x.xs」（不回传） |

> 语C功能需要配置可用的 OpenAI 兼容模型。关闭流式回复可兼容不支持 SSE 的接口；候选行动由模型生成，非流式模式下会额外发起一次请求，候选生成失败不影响正文回复。

---

## 应用预览

| 聊天列表 | 朋友圈 | 个人主页 |
|:---:|:---:|:---:|
| ![聊天列表](README_FILES/indexPage.jpg) | ![朋友圈](README_FILES/MomentsPage.jpg) | ![个人主页](README_FILES/MePage.jpg) |

| 自定义设置 | 聊天气泡设置 |
|:---:|:---:|
| ![自定义设置](README_FILES/CustomSettingsPage.jpg) | ![聊天气泡设置](README_FILES/ChatBubbleSettingPage.jpg) |

---

## 如何添加角色（以 CharactersImport 为例）

角色包是一个 `.zip` 文件，App 内通过 **【我】→ 导入角色包** 导入。一个 zip 中可以包含多个角色，每个角色是一个独立的文件夹。

项目根目录的 `CharactersImport\Sample1` 就是一份可直接打包成 zip 的示例：

```
CharactersImport/
└── Sample1/
    └── 爱弥斯/                  # 角色文件夹（文件夹名即角色名，可任意命名）
        ├── Profile.json         # 角色资料（必填）
        ├── Prompt.txt           # 角色提示词 / 人设（强烈建议提供）
        ├── ProfilePicture.jpg   # 角色头像（可选）
        ├── ProfileBackground.jpg# 角色详情页背景图（可选）
        └── moments/             # 朋友圈内容（可选，结构见「moments 文件夹说明」）
            ├── moments.json     # 朋友圈记录
            └── files/           # 朋友圈图片
                ├── 01.jpg
                └── ...
```

### 步骤

1. 在 `Sample1` 下新建一个文件夹，文件夹名即角色显示名（如 `爱弥斯`）；
2. 在文件夹内新建 `Profile.json`，填入角色资料；
3. （可选）新建 `Prompt.txt` 写入角色人设提示词；
4. （可选）放入头像图片 `ProfilePicture.jpg`（支持 jpg / jpeg / png / webp / gif / bmp，缺省时取角色目录内第一个图片文件）；
5. （可选）放入背景图 `ProfileBackground.jpg`（角色详情页封面背景图，缺省时详情页显示默认渐变）；
6. （可选）新建 `moments` 文件夹写入角色的朋友圈内容（格式见「moments 文件夹说明」）；
7. 将整个 `Sample1` 文件夹压缩为 zip；
8. 在 App 中进入 **【我】→ 导入角色包**，选择该 zip 并勾选要导入的角色。

### Profile.json 字段说明

| 字段 | 说明 | 是否必填 |
| ---- | ---- | ---- |
| `name` | 角色名称（缺省时使用文件夹名） | 建议 |
| `location` | 所在地（映射到「地区」） | 可选 |
| `gender` | 性别 | 可选 |
| `signature` | 个性签名 | 可选 |
| `remark` | 备注 | 可选 |
| `description` | 角色简介 | 可选 |
| `personality` | 性格特征 | 可选 |
| `greeting` | 开场白 | 可选 |
| `user_relationship` | 与用户的关系 | 可选 |
| `active_start` / `active_end` | 活跃时段，值为 `"HH:mm"`（如 `"09:00"`）；两者同时设置并生效后，在该时间段内聊天角色不会主动道别、说晚安，保持活跃；任一为空表示未设置 | 可选 |
| `tags` | 标签数组，如 `["鸣潮","电子幽灵"]` | 可选 |
| `avatar` | 内嵌头像（base64 字符串，存在时优先于图片文件） | 可选 |
| `background` | 内嵌背景图（base64 字符串，存在时优先于 `ProfileBackground.jpg`） | 可选 |
| `memory_points` | 持久化记忆数组（可选），元素为 `{"content": "记忆内容", "created_at": "创建时间"}`；导入角色包时写入该角色的记忆库 | 可选 |

`Sample1\爱弥斯\Profile.json` 示例：

```json
{
    "name": "爱弥斯",
    "location": "拉海洛·星炬学院",
    "gender": "女",
    "signature": "关注飞行雪绒喵~",
    "active_start": "00:00",
    "active_end": "23:59"
}
```

> **活跃时段设置示例**
> - **全时段活跃**：如上例，`active_start` 填 `"00:00"`、`active_end` 填 `"23:59"`，角色全天保持活跃，任何时间聊天都不会主动道别 / 说晚安；
> - **指定时段活跃**：如 `"09:00"` ~ `"23:00"`，仅在该时间段内保持活跃，时段外（如深夜）仍按角色人设作息判断是否道别；
> - 该字段不填（或任一项为空）即表示未设置活跃时段，保持原「按人设作息」逻辑。

### Prompt.txt 说明

`Prompt.txt` 的内容就是角色的 `systemPrompt`，App 在每次对话前会把它与用户资料、时间、输出格式指令一起组装成 System Prompt。

参考 `Sample1\爱弥斯\Prompt.txt`：建议写清楚角色**基础身份**、**性格特征**、**说话风格**、**核心记忆与执念**、**注意事项**，让角色行为稳定、性格鲜明。

### moments 文件夹说明（朋友圈）

角色包中可选的 `moments` 文件夹用于承载角色的**朋友圈内容**，结构与聊天记录导出包类似：`moments.json` 记录动态（文案 / 点赞 / 评论），`files/` 目录存放动态引用的图片。

```
moments/
├── moments.json   # 朋友圈记录（必填）
└── files/         # 朋友圈图片（由 images 相对路径引用）
    ├── 01.jpg
    └── ...
```

`moments.json` 根对象包含 `character_name`（角色名）与 `moments` 数组，数组内按时间倒序（新在前）排列各条动态：

| 字段 | 说明 |
| ---- | ---- |
| `character_name` | 角色名（根对象） |
| `moments` | 动态数组（根对象） |
| `id` | 动态唯一标识（可选，缺省自动生成） |
| `content` | 朋友圈正文文案 |
| `images` | 图片相对路径数组，如 `["files/01.jpg"]`，对应 `files/` 目录下的图片文件（可选） |
| `likes` | 点赞昵称数组，如 `["千咲","琳奈"]`（可选） |
| `comments` | 评论数组，每条为 `{"sender":"昵称","content":"评论内容"}`；需要表达「回复某人」时可增加 `"reply_to":"被回复昵称"`（可选，详见下方「带回复评论」） |
| `created_at` | 发布时间（ISO 8601 字符串，可选） |

`Sample1\爱弥斯\moments\moments.json` 示例：

```json
{
    "character_name": "爱弥斯",
    "moments": [
        {
            "id": "m1",
            "content": "这小手机怎么这么好玩~睡觉耽误玩手机，玩手机耽误睡觉。",
            "images": ["files/01.jpg"],
            "likes": ["千咲", "琳奈", "漂泊者"],
            "comments": [
                { "sender": "爱弥斯她老冯", "content": "不许玩了，快睡觉" },
                { "sender": "琳奈", "content": "姐们你太坑了" }
            ],
            "created_at": "2026-08-11T23:00:00.000"
        },
        {
            "id": "m2",
            "content": "老爹老妈的合影~还有老妈和我的合影~",
            "images": ["files/02.jpg", "files/03.jpg"],
            "likes": ["漂泊者", "千咲", "莫宁", "琳奈", "西格莉卡"],
            "comments": [
                { "sender": "莫宁", "content": "漂泊者这身很漂亮" }
            ],
            "created_at": "2026-08-05T22:30:00.000"
        }
    ]
}
```

#### 带回复评论（`reply_to`）

普通评论只需 `sender` + `content`；若要表达「某人回复了某人」，在评论对象里额外加一个 `reply_to` 字段，值为被回复者的昵称。App 内展示为 **「发送者 回复了 被回复者：评论内容」**。

```json
{
    "sender": "爱弥斯",
    "content": "不许玩了，快睡觉",
    "reply_to": "漂泊者"
}
```

- `reply_to` 为**可选**字段，缺省（或空字符串）时按普通评论展示「发送者：内容」；
- 旧版只有 `sender` + `content` 的评论 JSON 保持不变，与带回复评论互相兼容；
- 用户在朋友圈内**点击某条评论**即可套用「回复该昵称」的输入框，发送后生成带 `reply_to` 的评论；用户评论角色的朋友圈后，角色会以带 `reply_to` 的评论进行回复，回复者按以下规则确定：
  - 被回复的昵称**存在于通讯录**（且非"自己"）→ 以该角色身份回复；
  - 被回复的昵称不在通讯录 → 由该条动态的**发布者角色**回复；
  - 回复目标为"自己" → 不触发 AI 回复。
- 角色回复优先使用「朋友圈互动」模型（未配置时回退聊天模型）；若上下文估算已达到互动模型上下文窗口的 **70%**，会自动将最近聊天记录压缩为摘要后再请求模型。相关调试日志输出到 App 内「开发者日志」栏目。

- 图片文件支持 jpg / jpeg / png / webp / gif / bmp，放在 `files/` 目录下，由 `images` 字段以 `files/xxx.jpg` 的相对路径引用；单条动态最多展示 **9 张**（超出部分截断），1 张为大图、多张按 3 列网格排列（对齐微信朋友圈）；
- 没有 `moments` 文件夹时角色无朋友圈内容，详情页朋友圈区域显示空态；
- 文本文件建议使用 UTF-8 编码（与角色包其他文件一致）。

### 注意事项

- 文本文件建议使用 **UTF-8** 编码；程序对 GBK 乱码有兼容处理，但建议统一 UTF-8；
- zip 中**必须**包含至少一个带 `Profile.json` 的角色文件夹，否则会被过滤；
- 文件层级不要过深：`Sample1\角色A\Profile.json` 即可，不要把文件直接放在 zip 根目录。

---

## 朋友圈数据包（导入 / 导出）

App 内通过 **【我】→ 管理当前朋友圈** 管理朋友圈数据：顶部「导入朋友圈数据包」从 zip 导入；下方列表展示**已有朋友圈数据的角色**（可勾选）；底部「导出选中」把所选角色的全部朋友圈打包为 zip。**导入与导出的数据包结构一致，可互相回环**。

### 数据包结构

与角色包内 `moments/` 文件夹的内容格式相同：每个角色一个文件夹，`moments.json`（朋友圈记录）与图片文件夹（`files/`，由 `images` 相对路径引用）**直接放在角色文件夹下**（不含 `moments/` 外层包裹）。数据包**必须**以角色文件夹为单位，支持以下两种布局：

**布局一：带总包名（导出使用此结构）**

```
朋友圈_20260812_153000.zip
└── Moments/                    # 总包名（顶层文件夹，可任意命名）
    ├── 爱弥斯/                 # 角色文件夹（文件夹名即角色名）
    │   ├── moments.json        # 朋友圈记录
    │   └── files/              # 朋友圈图片
    │       ├── 01.jpg
    │       └── ...
    └── 其他角色/
```

**布局二：不带总包名（兼容导入）**

```
爱弥斯/
├── moments.json
└── files/
    ├── 01.jpg
    └── ...
```

两种布局均可导入；多个角色放同一个 zip 时建议统一使用同一布局。`moments.json` 的字段格式与上文「moments 文件夹说明」一致（`character_name` + `moments` 数组）。

### 导入规则

- 按**角色名**（备注/昵称）匹配已有角色：匹配到则**更新**该角色的朋友圈（整体替换）；未匹配到则新建一个角色（仅名称 + 朋友圈，可在「管理当前角色」中补充头像等资料）；
- 图片从数据包 `files/` 目录提取到应用本地，`images` 字段自动改写为本地路径；
- zip 中找不到任何 `moments.json` 时提示导入失败。

### 导出规则

- 在「管理当前朋友圈」勾选角色后点击「导出选中」，生成 `朋友圈_yyyyMMdd_HHmmss.zip`（保存到下载目录）；
- 导出结构为**布局一**（总包名 `Moments` + 角色文件夹），`moments.json` 与 `files/` 直接位于角色文件夹下，图片写入 `files/` 并以 `files/xxx.jpg` 相对路径引用，可直接再次导入；
- 仅导出有朋友圈数据的角色；单个角色的导出内容即其全部朋友圈动态。

---

## 持久化记忆（保存与导入 / 导出）

持久化记忆（记忆点）是用户主动保存的、需要模型在后续对话中长期记住的关键信息：在聊天中**长按消息 → 多选 → 保存为记忆点**，或在会话详情「提示词设置 → 记忆点管理」中手动添加 / 编辑 / 删除。记忆点会随角色的**系统提示词**一起发送给模型，聊天与朋友圈互动（自动发帖 / 点赞评论 / 回复评论）均生效。

### 保存格式（App 内）

每个角色独立一组记忆点，持久化在 SharedPreferences，键为 `memory_points_v1_<角色id>`，值为按创建时间**倒序（最新在前）**排列的 JSON 数组：

```json
[
  {
    "id": "f3a9c2e0-0000-0000-0000-000000000001",
    "content": "用户喜欢深夜聊天，不喜欢被催促睡觉",
    "created_at": "2026-08-01T21:30:00.000"
  }
]
```

| 字段 | 说明 |
| ---- | ---- |
| `id` | 记忆点唯一标识（角色包中不包含，导入时会重新生成） |
| `content` | 记忆内容 |
| `created_at` | 创建时间（ISO 8601 字符串） |

持久化记忆随**角色包**一起导入 / 导出：导出角色包时，记忆点自动写入 `Profile.json` 的 `memory_points` 字段；导入角色包时自动恢复到对应角色，无需额外操作。

### 角色包中的格式（Profile.json）

角色包内每个角色的 `Profile.json` 新增可选字段 `memory_points`，数组元素为 `{"content": "...", "created_at": "..."}`：`content` 为记忆文本，`created_at` 为 ISO 8601 创建时间（可省略，缺省按导入时间）。导出角色包时自动写入，角色没有记忆点时不生成该字段。

`Sample1\爱弥斯\Profile.json` 示例：

```json
{
    "name": "爱弥斯",
    "location": "拉海洛·星炬学院",
    "signature": "关注飞行雪绒喵~",
    "memory_points": [
        {
            "content": "用户喜欢深夜聊天，不喜欢被催促睡觉",
            "created_at": "2026-08-01T21:30:00.000"
        },
        {
            "content": "约好周末一起看新出的电影"
        }
    ]
}
```

### 导入规则

导入角色包（【我】→ 管理当前角色 → 导入角色包，或创意工坊的「角色」分类 zip）时，记忆点随角色一起恢复：

- **新角色**：包内记忆点直接写入该角色（保留原创建时间）；
- **同名角色**（覆盖导入）：包内记忆点**整体替换**该角色已有的记忆（与资料、提示词、朋友圈的处理一致，聊天记录不受影响）；
- 角色包无 `memory_points` 字段或数组为空：不改变该角色已有的记忆；
- 记忆点内容相同的自动去重。

---

## 群聊功能

### 创建群聊

在通讯录页面点击右上角「+」→「创建群聊」，选择多个角色加入群聊。群聊支持：

- **群名称**：自定义群聊名称
- **群头像**：支持自定义群头像，未设置时自动生成成员拼图头像
- **群简介**：设置群聊简介，角色会按简介语境进行对话
- **群成员**：选择多个角色加入群聊

### 群聊设置

进入群聊后，点击右上角「...」→「群聊设置」可配置：

- **上下文条数**：群聊可独立设置上下文条数，或跟随全局设置
- **不说话概率**（0~1，默认 0.2）：每轮每个未被 @ 的角色以此概率保持沉默，被 @ 的角色必定发言
- **引用回复概率**（0~1，默认 0.2）：角色发言时以此概率引用最近其他成员的消息

### 群聊交互

- **@ 角色**：在输入框中输入 `@角色名` 可 @ 特定角色，被 @ 的角色必定发言
- **引用回复**：长按消息可引用回复，角色发言时可引用其他成员的消息
- **未读消息**：群聊支持未读消息计数，离开群聊页面时累计新消息

### 群成员管理

- **群成员记忆**：可为每个群成员单独管理记忆点
- **群成员模型**：可为每个群成员单独配置使用的模型

---

## 创意工坊

创意工坊用于从 GitHub/Gitee 仓库或 COS 类对象储存下载角色包、游戏包和表情包。

### 入口

**【我】→ 创意工坊**

### 添加仓库

1. 点击右上角「仓库管理」
2. 点击右上角 **+**
3. 粘贴或输入地址，App 会自动识别来源类型（弹窗内也可手动切换）：
   - **Git**：`owner/repo` 或 GitHub / Gitee 完整 URL，可选配置下载代理
   - **对象储存**：`https://` 的 BASE_URL（需桶允许匿名 ListObjects + GetObject）
4. 保存后自动检测可用资产

**对象储存 BASE_URL 示例**：

```text
https://aichatapp-1234567890.cos.ap-guangzhou.myqcloud.com
https://my-bucket.oss-cn-hangzhou.aliyuncs.com
https://my-bucket.oss-cn-hangzhou.aliyuncs.com/aichat
```

可以把整个桶根当作 BASE_URL，也可以把某个前缀目录（如 `/aichat`）当作 BASE_URL。

### 资产分类

- **角色分类（V1.1.0）**：包含 `Profile.json` 的角色文件夹 zip，可直接导入为角色
- **游戏分类（V1.0.0）**：包含 `moments.json` 的朋友圈数据包，可导入为角色朋友圈
- **表情包分类（V1.3.0）**：包含图片文件的表情包 ZIP，可导入到本地表情包库
- **更新通知（V1.2.0）**：接收仓库更新通知

### COS 对象储存目录约定

使用 COS / OSS / S3 兼容对象储存时，App 按 BASE_URL 下的固定目录自动发现资产：

```text
{BASE_URL}/
├── Characters/*.zip   # 角色分类
├── Games/*.zip        # 游戏分类（朋友圈数据包）
├── Stickers/*.zip     # 表情包分类
└── Note/*.md          # 更新通知（Markdown，弹窗完整展示）
```

| 目录 | 资产 | 映射分类 |
| ---- | ---- | -------- |
| `Characters/` | `.zip` | 角色分类（V1.1.0） |
| `Games/` | `.zip` | 游戏分类（V1.0.0） |
| `Stickers/` | `.zip` | 表情包分类（V1.3.0） |
| `Note/` | `.md` | 更新通知（V1.2.0） |

**Note 优先级**：`update.md` → `note.md` → `readme.md` → 目录中最后一个 `.md`。

**列表方式**：S3 ListObjects V2。打开分类时先取首页约 500 条；搜索或点「加载更多」时按 `continuation-token` 拉全量（默认最多约 10 页）。List 结果缓存约 30 分钟。

**变更刷新**：进分类时会用 `Note/*.md` 做变更探测；Note 内容变化才强制重新 List。也可在仓库管理中手动刷新。

**权限要求**：默认桶需允许匿名 `ListObjects`（GET Bucket）与 `GetObject`（GET Object）。也可在添加 COS 仓库时开启「使用访问密钥（私有读）」，用 AccessKey 做请求签名（支持腾讯云 COS、阿里云 OSS），此时桶可保持私有读。密钥仅保存在本机。只开 Get 不开 List 且未配置密钥时无法枚举 zip，App 会提示 HTTP 403。

---

## 自行配置对象储存（COS / OSS）

本教程教你用自己的对象储存搭一个创意工坊来源，无需自建服务器。以**腾讯云 COS** 与**阿里云 OSS** 为例；其他兼容 S3 ListObjects 的服务（MinIO、AWS S3 等）原理相同。

### 通用步骤概览

1. 创建存储桶（Bucket）
2. 上传目录结构（`Characters/`、`Games/`、`Stickers/`、`Note/`，需要哪类就传哪类）
3. 开启匿名可读：至少允许 **列表对象** + **读取对象**
4. 把访问域名填进 App 的「添加仓库 → 对象储存」

---

### 腾讯云 COS

#### 1. 创建存储桶

1. 打开 [腾讯云 COS 控制台](https://console.cloud.tencent.com/cos/bucket)
2. **创建存储桶**
   - 地域：例如 `ap-guangzhou`（建议选离用户近的地域）
   - 访问权限：建议先选 **公有读私有写**（可后续再收紧）
3. 记下访问域名，形如：

```text
https://<bucket-name>-<appid>.cos.<region>.myqcloud.com
# 例：https://aichatapp-1398802649.cos.ap-guangzhou.myqcloud.com
```

#### 2. 上传目录与资产

在控制台「文件列表」或使用 COSBrowser / coscli 上传：

```text
Characters/三月七.zip
Games/示例朋友圈包.zip
Stickers/表情包.zip
Note/update.md
```

角色包 zip 内结构参见上文「如何添加角色」；表情包打包标准参见下节。

#### 3. 配置匿名访问（关键）

App 使用匿名 HTTP 访问，不需要 SecretId/SecretKey。

**方式 A：公有读私有写（最简单）**

存储桶 → 权限管理 → 访问公共权限 → **公有读私有写** → 保存。

> 该设置会同时允许匿名「列表对象」和「读取对象」。

**方式 B：Bucket Policy（更精细）**

存储桶 → 权限管理 → Policy 设置 → 新增策略，示例（把资源路径换成你的桶）：

```json
{
  "version": "2.0",
  "statement": [
    {
      "effect": "allow",
      "principal": {
        "qcs": ["qcs::cam::anyone:anyone"]
      },
      "action": [
        "name/cos:GetObject",
        "name/cos:ListBucket"
      ],
      "resource": [
        "qcs::cos:ap-guangzhou:uid/1398802649:aichatapp-1398802649.ap-guangzhou.myqcloud.com/*",
        "qcs::cos:ap-guangzhou:uid/1398802649:aichatapp-1398802649.ap-guangzhou.myqcloud.com"
      ]
    }
  ]
}
```

说明：

| 动作 | 对应 API | 作用 |
| ---- | -------- | ---- |
| `name/cos:GetObject` | GET Object | 下载 zip / md |
| `name/cos:ListBucket` | GET Bucket（查询对象列表） | 列出 `Characters/` 等目录 |

只开 GetObject、不开 ListBucket 时，App 会报 **HTTP 403**，无法发现资产。

#### 4. 在 App 中添加

创意工坊设置 → **+** → 粘贴：

```text
https://aichatapp-1398802649.cos.ap-guangzhou.myqcloud.com
```

保存后应能看到「角色 / 游戏 / 表情包」分类 chip；若只有「更新通知」，通常说明 List 权限仍未打开。

#### 5. 发布更新通知

在桶内创建 `Note/update.md`（或 `note.md` / `readme.md`），内容为 Markdown。变更文件内容后，把该 COS 仓库设为通知源，下次启动会完整弹窗展示。

---

### 阿里云 OSS

#### 1. 创建 Bucket

1. 打开 [阿里云 OSS 控制台](https://oss.console.aliyun.com/bucket)
2. **创建 Bucket**
   - Region：例如华东 1（杭州）
   - 读写权限：建议先选 **公共读**
3. 记下外网 Endpoint 与访问域名，形如：

```text
https://<bucket-name>.oss-cn-hangzhou.aliyuncs.com
```

若只把某个前缀当作工坊根目录，BASE_URL 可写成：

```text
https://<bucket-name>.oss-cn-hangzhou.aliyuncs.com/aichat
```

对应对象前缀为 `aichat/Characters/...` 等。

#### 2. 上传目录与资产

与 COS 相同，保持固定目录名（大小写敏感）：

```text
Characters/*.zip
Games/*.zip
Stickers/*.zip
Note/*.md
```

可用控制台拖拽上传，或 `ossutil`：

```bash
ossutil cp -r ./Characters oss://my-bucket/Characters/
ossutil cp ./Note/update.md oss://my-bucket/Note/update.md
```

#### 3. 配置匿名访问（关键）

**方式 A：Bucket 读写权限为「公共读」（最简单）**

Bucket → 权限管理 → 读写权限 → **公共读**。

**方式 B：Bucket 授权策略（更精细）**

Bucket → 权限管理 → Bucket 授权策略 → 新增授权：

- 授权用户：**全部账号（包括匿名访问）**
- 授权资源：整个 Bucket 或指定前缀 `my-bucket/*`
- 授权操作：至少勾选
  - `oss:ListObjects`（列表对象）
  - `oss:GetObject`（读取对象）

> 阿里云「公共读」通常已包含匿名 List + Get；若只对单个文件做了公共读而未开放 List，App 同样无法枚举资产。

#### 4. 在 App 中添加

```text
https://my-bucket.oss-cn-hangzhou.aliyuncs.com
```

或：

```text
https://my-bucket.oss-cn-hangzhou.aliyuncs.com/aichat
```

#### 5. CORS（可选）

App 走的是原生 HTTP，一般**不需要**配置 CORS。若你用网页端预览桶内容并跨域访问，再在「数据安全 → 跨域设置」中添加允许来源。

---

### 自检清单

配置完成后，可用浏览器或命令行快速验证：

```bash
# 应返回 XML 列表（200），而不是 403/400
curl -i "https://你的桶域名/?list-type=2&max-keys=1000"

# 应能直接下载
curl -I "https://你的桶域名/Note/update.md"
```

| 现象 | 可能原因 |
| ---- | -------- |
| HTTP 403 | 未允许匿名 ListBucket / GetObject |
| HTTP 400 | 请求参数异常；或 BASE_URL / 域名写错 |
| 保存成功但只有「更新通知」 | Note 可 Get，但 List 仍不可用，无法枚举 zip |
| 分类出现但下载失败 | 单个对象未对匿名开放，或 URL 中文未正确编码 |
| 一直空列表 | 目录名不是 `Characters` / `Games` / `Stickers` / `Note`（区分大小写），或 zip 放在了子目录里 |

### 安全建议

- 该桶**只放**可公开的角色包 / 表情包 / 通知，不要上传密钥、隐私数据
- 不需要公网 List 时，可将 BASE_URL 收窄到某个前缀，并只对该前缀授权
- 付费/私有内容请另寻鉴权方案：当前实现仅支持匿名 List + Get

---

## 创意工坊表情包 ZIP 打包标准

创意工坊的「表情包分类（V1.3.0）」下载资产必须是 `.zip` 压缩包。App 会解压其中所有支持格式的图片，导入到本地表情包库；图片的**文件名（去掉扩展名）会自动保存为表情包备注**，用于表情包管理、本地检索和角色按语义发送表情包。

### 支持的图片格式

| 格式 | 扩展名 | 说明 |
| ---- | ------ | ---- |
| GIF 动图 | `.gif` | 聊天界面和表情包预览中会按动图播放 |
| WebP | `.webp` | 支持静态 WebP；动画 WebP 的实际播放效果取决于设备系统图像解码能力 |
| PNG | `.png` | 支持透明背景 |
| JPEG | `.jpg` / `.jpeg` | 常规静态图片 |

> 不支持的文件会被自动忽略；无需提供额外的 JSON、清单文件或同名 `.txt` 备注文件。

### 文件夹结构

ZIP 内可直接放图片，也可以像普通表情包一样用一个最外层文件夹包裹图片。App 会递归扫描 ZIP 内各层目录，因此以下两种布局都支持。

**推荐布局：使用与压缩包同名的文件夹**

```text
大肥鱼表情包.zip
└── 大肥鱼表情包/
    ├── 大肥鱼_烦恼醉酒中.gif
    ├── 大肥鱼_要饭.jpg
    └── 大肥鱼_顶盆子.jpg
```

**也可直接将图片放在 ZIP 根目录**

```text
表情包合集.zip
├── 熊猫头_无语.png
├── 小狗_开心.webp
└── 猫猫_晚安.gif
```

### 备注命名规则

图片文件名去除最后的扩展名后，会原样保存为备注；中文、数字、空格、下划线和常规符号都会保留。

| ZIP 内文件 | 导入后的表情包备注 |
| ---------- | ------------------ |
| `大肥鱼_烦恼醉酒中.gif` | `大肥鱼_烦恼醉酒中` |
| `熊猫头 无语.png` | `熊猫头 无语` |
| `cat-happy.webp` | `cat-happy` |

建议使用能描述表情含义、情绪或使用场景的文件名，例如 `猫猫_委屈想哭.gif`、`熊猫头_上班摸鱼.jpg`。这样角色请求 `[[查询表情包:委屈]]` 或 `[[查询表情包:上班摸鱼]]` 时，本地检索更容易命中正确的表情包。

### 打包与发布步骤

1. 新建一个表情包文件夹，例如 `大肥鱼表情包`；
2. 将 GIF、WebP、PNG、JPG/JPEG 图片放入该文件夹，并按语义为每张图片命名；
3. 压缩**文件夹本身**为 ZIP，例如 `大肥鱼表情包.zip`；
4. 发布到创意工坊来源：
   - **Git**：在仓库的 `V1.3.0` Release 中上传该 ZIP 作为资产；
   - **对象储存**：上传到 `Stickers/大肥鱼表情包.zip`；
5. 用户在 App 的 **【我】→ 创意工坊 → 表情包分类** 勾选并下载导入。

导入完成后，表情包会显示在聊天输入框的表情包面板中，并可在 **【我】→ 设置 → 管理表情包** 查看、编辑或删除。

### 更新通知

- 在仓库管理页面可开启「更新通知」，并选择通知仓库
- **Git**：启动时检查是否有新的 `V1.2.0` Release；Release 描述变化时弹窗
- **对象储存**：启动时拉取 `Note/*.md`（优先 `update.md`）；**内容变化才提醒**（按仓库记录已提醒指纹，归一化换行后对比）；弹窗完整展示 Markdown

---

## 自动发朋友圈与主动问候

### 自动发朋友圈

角色可按设定的周期自动生成并发布朋友圈内容。

**配置入口**：角色详情页 → 自动发朋友圈

- **启用**：开启后角色会按周期自动发布朋友圈
- **发布周期**（小时）：每隔多少小时发布一条，默认 72 小时（3 天）
- **周期内发布条数**：每个周期内发布的朋友圈条数，默认 1 条
- **可见范围**：设置自动发布的朋友圈可见范围（仅自己/全部/自定义分组）

**触发机制**：

- 应用启动 / 回到前台时检查并补发布
- 低频（一周 <7 条）补发积压的上限：最多一次补发 3 条
- 全程串行，一次只发一个 LLM 请求
- 失败静默跳过并记录日志，不弹 Toast 干扰用户

### 主动问候

角色在用户一段时间未聊天后，会主动发送问候消息。

**配置入口**：角色详情页 → 主动问候

- **启用**：开启后角色会主动发送问候消息
- **无消息触发时间**（小时）：用户多少小时未与角色聊天后触发，默认 72 小时（3 天）

**触发机制**：

- 应用启动 / 回到前台时检查是否到期
- 到期后角色会发送一条问候消息
- 消息会推送到系统通知栏

---

## 聊天背景与 UI 样式

### 聊天背景

为每个会话设置独立的背景图片和模糊度。

**配置入口**：聊天页面 → 右上角「...」→ 设置聊天背景

- **选择图片**：从相册选择图片作为聊天背景
- **模糊度**：调节背景图片的高斯模糊度（0 = 不模糊，默认 10）
- **重置**：清除当前会话的背景设置

**特性**：

- 每个会话（私聊/群聊）独立设置
- 背景图片复制到应用文档目录持久化
- 支持浅色/深色模式

### UI 样式

**配置入口**：**【我】→ 设置 → UI 样式**

- **默认**：顶部居中标题 + 常规输入栏
- **终末地**：标题靠左 + 个性签名副标题 + 在线状态点（绿/红），发送按钮深底金边白字

### 气泡样式

**配置入口**：**【我】→ 设置 → 气泡样式**

- **默认**：矩形圆角 + 描边，背景色可自定义
- **崩铁样式**：大圆角 + 柔和阴影，自带配色（自己=暖棕，对方=浅灰）
- **鸣潮样式**：上边共线尾巴 + 大圆角弧线，自带配色
- **终末地样式**：基于鸣潮的尾巴形状，三角均 15px、尾巴回程弧 10px，浅深配色一致

**自定义颜色**：

- 可分别设置自己/对方 × 浅色/深色的气泡颜色
- 可分别设置自己/对方 × 浅色/深色的气泡内字体颜色

---

## Token 消耗统计

记录各会话（私聊/群聊/朋友圈互动）的累计 token 使用量。

**入口**：**【我】→ Token 消耗统计**

### 统计内容

- **输入 tokens**：累计发送给模型的 token（API `prompt_tokens` 累加）
- **输出 tokens**：累计从模型接收的 token（API `completion_tokens` 累加，**已含思考**，对齐账单）
- **其中思考 tokens**：输出中的思考过程部分；优先取 API `completion_tokens_details.reasoning_tokens`，否则按思考正文估算；**不重复计入总额**
- **总 tokens**：输入 + 输出

### 数据来源

- **私聊**：按会话 id（conversationId）统计
- **群聊**：按群聊 id（groupId）统计
- **朋友圈互动**：统一归入 `moment_interactions` 统计 id

### 操作

- **查看**：按会话分类展示 token 使用量；有思考时副标题会标出「（思考 …）」
- **重置**：可一键重置所有会话的 token 统计

---

## 记忆池（跨场景记忆）

聚合角色在「当前会话之外」的近期记忆，让角色在私聊/群聊/朋友圈互动中保持跨场景的记忆连贯（类似真人好友记得你私聊说过什么、群里发过什么、朋友圈发过什么）。

### 记忆池内容

1. **近期私聊内容**：条数 = 全局上下文设置；私聊场景下已作为对话历史传入，跳过
2. **近期朋友圈**：最近 N 条贴文 + 全部点赞与评论（N = 全局「朋友圈记忆条数」，默认 3，0=不拼）
3. **近期群聊内容**：该角色参与的、最近有消息的 1 个群，每群最近 3 条文本消息；群聊场景排除当前群，避免与对话历史重复
4. **角色资料卡**：昵称/关系/备注/签名/地区/性格/介绍/开场白/标签，只拼非空字段
5. **用户资料**：昵称/性别/地区/签名，需传入 User，只拼非空字段

### 配置

**入口**：**【我】→ 设置 → 记忆池管理**

- **朋友圈记忆条数**：每个角色最近 N 条朋友圈贴文（默认 3，0=不拼入）
- **按角色停用来源**：可为每个角色独立停用记忆池的各个来源（近期私聊/近期朋友圈/近期群聊/角色资料卡）

---

## 朋友圈可见性分组

自定义朋友圈可见范围分组。

**入口**：**【我】→ 管理当前角色 → 可见性分组管理**

### 可见范围选项

- **仅自己可见**：发布的朋友圈只有自己能看到
- **全部角色可见**（默认）：所有角色都能看到
- **自定义分组**：选择特定角色组成的分组

### 分组管理

- **创建分组**：输入分组名称，选择成员角色
- **编辑分组**：修改分组名称和成员
- **删除分组**：删除自定义分组

### 使用场景

- 发布朋友圈时可选择可见范围
- 自动发朋友圈可配置默认可见范围
- 不同的朋友圈可设置不同的可见范围

---

## 聊天记录导出 / 导入

支持将聊天记录导出为 zip 包，可导入恢复。

**入口**：聊天详情页面 → 导出聊天记录 / 导入聊天记录

### 导出格式

```
聊天记录_角色名_20260812_153000.zip
├── chat.json        # 聊天记录（文本消息 content 为正文）
├── images/          # 图片消息的原始图片文件
│   ├── image_0.jpg
│   └── ...
└── files/           # 文件消息的原始文件
    ├── 原文件名.pdf
    └── ...
```

### 导出内容

- **文本消息**：content 为正文
- **图片消息**：原始图片文件打包到 images/ 目录
- **文件消息**：原始文件打包到 files/ 目录
- **系统消息**：如「你已添加了...」等系统提示
- **合并转发**：保留 forwarded_items 结构
- **引用消息**：保留 quote_content 和 quote_sender
- **群聊消息**：保留 sender_character_id 和 sender_name

### 导入规则

- 按消息 id 去重，避免重复导入
- 图片/文件自动解压到应用本地目录
- 支持私聊和群聊记录导入

---

## 存储空间管理

管理应用占用的存储空间。

**入口**：**【我】→ 设置 → 存储管理**

### 存储分类

- **用户数据**：
  - 聊天记录
  - 角色数据
  - 朋友圈数据
  - 记忆点
- **软件缓存**：
  - 创意工坊下载缓存
  - 图片缓存
  - 其他临时文件

### 操作

- **查看详情**：查看各分类的占用大小
- **清理缓存**：清理软件缓存（不影响用户数据）
- **清理用户数据**：清理聊天记录、朋友圈图片等（需确认）

### 其他应用文件

- 自动分析文件类型，安全删除散文件和临时目录
- 保留包含数据文件的目录（无法确认安全）
- 显示将保留的目录名称供用户了解

---

## 项目结构

```
lib/
├── main.dart                      # 入口
├── app.dart                       # 应用根组件（Provider 注入、主题）
├── config/
│   ├── routes.dart                # 路由表
│   └── theme.dart                 # 主题色板与扩展
├── models/                        # 数据模型
│   ├── character.dart             # 角色模型
│   ├── message.dart               # 消息模型（文本/图片/文件/表情包/系统/剧情行动/合并转发）
│   ├── conversation.dart          # 私聊会话
│   ├── group_chat.dart            # 群聊模型
│   ├── moment.dart                # 朋友圈动态
│   ├── moment_notification.dart   # 朋友圈互动通知
│   ├── moments_pack_entry.dart    # 朋友圈数据包条目
│   ├── character_pack_entry.dart  # 角色包条目
│   ├── memory_point.dart          # 记忆点
│   ├── user.dart                  # 用户模型
│   ├── token_usage.dart           # Token 消耗统计
│   ├── visibility_group.dart      # 朋友圈可见性分组
│   ├── provider_preset.dart       # 厂商预设
│   ├── workshop_asset.dart        # 创意工坊资产
│   └── workshop_repository.dart   # 创意工坊仓库
├── providers/                     # 状态管理
│   ├── auth_provider.dart         # 用户认证
│   ├── api_provider.dart          # API 模型配置
│   ├── chat_provider.dart         # 私聊聊天
│   ├── character_provider.dart    # 角色管理
│   ├── settings_provider.dart     # 全局设置（主题/气泡/UI样式）
│   ├── chat_settings_provider.dart# 聊天设置（上下文/压缩/记忆池）
│   ├── group_chat_provider.dart   # 群聊
│   ├── memory_point_provider.dart # 记忆点管理
│   ├── token_usage_provider.dart  # Token 消耗统计
│   ├── auto_moment_provider.dart  # 自动发朋友圈配置
│   ├── proactive_greeting_provider.dart # 主动问候配置
│   ├── chat_background_provider.dart    # 聊天背景
│   ├── moment_notification_provider.dart# 朋友圈通知
│   └── workshop_provider.dart     # 创意工坊
├── screens/                       # 页面
│   ├── home_screen.dart           # 主页（聊天列表/通讯录/朋友圈/我）
│   ├── chat_screen.dart           # 私聊页面
│   ├── chat_detail_screen.dart    # 聊天详情/设置
│   ├── chat_search_screen.dart    # 聊天搜索
│   ├── chat_settings_screen.dart  # 聊天设置
│   ├── group_chat_screen.dart     # 群聊页面
│   ├── group_chat_detail_screen.dart    # 群聊详情
│   ├── group_chat_settings_screen.dart  # 群聊设置
│   ├── group_member_memory_screen.dart  # 群成员记忆
│   ├── group_member_model_screen.dart   # 群成员模型
│   ├── create_group_screen.dart   # 创建群聊
│   ├── contacts_search_screen.dart# 联系人搜索
│   ├── character_list_screen.dart # 角色列表
│   ├── character_detail_screen.dart     # 角色详情
│   ├── character_manage_screen.dart     # 角色管理
│   ├── character_import_screen.dart     # 角色导入
│   ├── moments_screen.dart        # 朋友圈
│   ├── moments_manage_screen.dart # 朋友圈管理
│   ├── moment_notifications_screen.dart # 朋友圈通知
│   ├── moment_visibility_screen.dart    # 朋友圈可见性
│   ├── publish_moment_screen.dart       # 发布朋友圈
│   ├── profile_screen.dart        # 个人主页
│   ├── profile_edit_screen.dart   # 编辑个人资料
│   ├── settings_screen.dart       # 设置页面
│   ├── api_settings_screen.dart   # API 设置
│   ├── model_edit_screen.dart     # 模型编辑
│   ├── provider_config_screen.dart# 厂商配置
│   ├── provider_preset_screen.dart# 厂商预设
│   ├── token_usage_screen.dart    # Token 消耗统计
│   ├── storage_manage_screen.dart # 存储管理
│   ├── bubble_style_screen.dart   # 气泡样式
│   ├── ui_style_screen.dart       # UI 样式
│   ├── splash_icon_screen.dart    # 启动图标
│   ├── splash_screen.dart         # 启动页
│   ├── forward_detail_screen.dart # 合并转发详情
│   ├── image_crop_screen.dart     # 图片裁剪
│   ├── memory_point_manage_screen.dart  # 记忆点管理
│   ├── memory_pool_manager_screen.dart  # 记忆池管理
│   ├── visibility_group_manage_screen.dart # 可见性分组管理
│   ├── workshop_screen.dart       # 创意工坊
│   └── workshop_repos_screen.dart # 创意工坊仓库管理
├── services/                      # 业务服务
│   ├── llm_service.dart           # LLM 调用（生成回复 / 压缩摘要）
│   ├── update_service.dart        # Gitee/GitHub 双源更新检测、APK 下载与安装
│   ├── prompt_builder.dart        # System Prompt 与输出指令组装
│   ├── character_pack_service.dart# 角色包 zip 解析与导出
│   ├── chat_records_service.dart  # 聊天记录导出/导入
│   ├── workshop_service.dart      # 创意工坊服务
│   ├── moment_ai_service.dart     # 朋友圈 AI 互动引擎
│   ├── auto_moment_service.dart   # 自动发朋友圈调度
│   ├── memory_pool_builder.dart   # 记忆池构建器
│   ├── notification_service.dart  # 系统通知服务
│   ├── storage_manager_service.dart # 存储空间管理
│   └── dev_log_service.dart       # 开发者日志
├── utils/                         # 工具类
│   ├── app_toast.dart             # Toast 提示
│   ├── character_pack_picker.dart # 角色包文件选择
│   ├── conversation_relink.dart   # 会话重关联
│   ├── file_picker_helper.dart    # 文件选择器
│   ├── file_utils.dart            # 文件工具
│   └── pinyin_util.dart           # 拼音工具
└── widgets/                       # 公共组件
    ├── alphabet_index_bar.dart    # 字母索引栏
    ├── character_avatar.dart      # 角色头像组件
    ├── chat_bubble.dart           # 聊天气泡
    ├── chat_send_button.dart      # 发送按钮
    ├── chat_title_bar.dart        # 聊天标题栏
    ├── message_input.dart         # 消息输入框
    ├── moment_card.dart           # 朋友圈卡片
    ├── publish_moment_screen.dart # 发布朋友圈
    ├── splash_icon_view.dart      # 启动图标视图
    └── update_dialogs.dart        # 更新弹窗
```

---

## 修改应用版本号（V1.0.0）

> 约定：版本号完全由**开发者自行设置**，没有固定递增规则。在 `pubspec.yaml` 中自由填一个版本号即可，GitHub Release 的 `tag` 与它保持一致。

版本号定义在项目根目录的 `pubspec.yaml`：

```yaml
version: 1.0.0
```

- 开发者自由填写版本号（如 `1.0.0`、`1.1.0`），App 内「软件版本」显示为 `V1.0.0`；
- 不需要 `1.0.0+1` 这种带 build 号的写法，一个数字即可。

发布流程：

1. 开发者在 `pubspec.yaml` 中填写想要的版本号，例如 `1.0.0` → `1.1.0`；
2. 执行 `build_apk.bat` 打包（产物为 `build\app\outputs\flutter-apk\AiChat-V1.1.0.apk`）；
3. 在 Gitee 与 GitHub 仓库各新建一个 Release：`tag` 填与 pubspec 相同的版本号（如 `1.1.0`，可带可不带 `v` 前缀；必须比本地版本新，更新检测据此提示升级），并上传 **按命名标准命名** 的 APK 资产 `AiChat-V1.1.0.apk`。

> 说明：App 内「软件版本」通过 `package_info_plus` 读取，`flutter build apk` 会把 pubspec 中的版本写入 APK，无需手动改 AndroidManifest。

---

## 本地打包 APK

项目根目录已提供一键打包脚本 **`build_apk.bat`**（需已安装 Flutter 并配置 PATH）：

```bat
:: 打包 debug 包
build_apk.bat debug

:: 打包 release 包（默认，按 ABI 拆分）
build_apk.bat
```

脚本会自动执行 `flutter pub get` → `flutter build apk --release --split-per-abi`，并按 ABI 拆分输出 APK：

```text
build\app\outputs\flutter-apk\
├── AiChat-V1.0.0.apk                # 向后兼容版（arm64-v8a 复制）
├── AiChat-V1.0.0-arm64-v8a.apk      # ARM64 设备
├── AiChat-V1.0.0-armeabi-v7a.apk    # 32 位 ARM 设备
└── AiChat-V1.0.0-x86_64.apk         # x86_64 模拟器/设备
```

**说明**：
- `AiChat-V1.0.0.apk`（无 ABI 后缀）是 `arm64-v8a` 的复制，用于向后兼容（部分下载源要求固定文件名）
- 大多数现代 Android 手机使用 `arm64-v8a`
- 发布到 GitHub/Gitee Release 时，建议上传 `AiChat-V1.0.0.apk`（兼容版）

也可以手动执行：

```bash
flutter pub get
flutter build apk --release --split-per-abi
# 产物位于 build\app\outputs\flutter-apk\
```

---

## 自动更新机制

- **版本检查（双源）**：
  1. 优先 Gitee：默认 `https://gitee.com/Murchey/AiChatApp`（国内直连）；
  2. 备用 GitHub：默认 `https://github.com/Niriko-mu/AiChat`（可叠加加速代理）。
  检测仓库可在 **设置 → 更新 → Gitee / GitHub 更新仓库** 中分别自定义（支持完整 URL 或 `owner/repo`，持久化保存）。
  对比 `tag_name` 与本地版本号，取 Gitee 优先的版本与更新说明；下载链接随所配置的仓库同步变化；
- **下载源选项卡**：发现新版本弹窗内提供「下载源」选项卡，**默认首选 Gitee**、其次 GitHub；GitHub 源可叠加加速代理下载；
- **APK 资产命名标准**：`AiChat-V1.0.0.apk`（`AiChat-V<版本号>.apk`），检测时优先取符合标准的资产，无资产时按此命名拼接下载直链；
- **安装**：下载到应用外部目录 `updates/`，通过原生 FileProvider + 系统安装器安装（需授权「安装未知应用」）；
- 入口：
  - **【我】→ 软件版本**：手动检查更新；
  - **【我】→ 设置 → 启动时自动检测更新**：开启后每次启动自动检测；
  - **【我】→ 设置 → Gitee / GitHub 更新仓库**：分别配置检测与下载所用仓库；
  - **【我】→ 设置 → GitHub 加速地址**：选择/自定义加速代理（仅影响 GitHub 源下载）。

发布新版本流程：在 **Gitee 与 GitHub 仓库**各新建一个 Release，`tag` 填与 `pubspec.yaml` 相同的版本号（可带 `v` 前缀，需比本地版本新），并上传按命名标准命名的 APK 资产 `AiChat-V<版本号>.apk` 即可。

---

## API 设置与压缩会话

### API 设置（【我】→ API 设置）

- 添加模型：支持任意 OpenAI 兼容接口，填「展示名称 / 模型名称 / 接口地址 / API Key / 上下文长度(token)」；
- **厂商预设**：内置多个 AI 厂商配置预设（OpenAI、Google Gemini、小米 MiMo 等），一键填入配置
- **模型检测**：支持一键检测可用模型列表
- 为每个模型配置**上下文长度**，作为会话压缩 70% 阈值的基准；
- **压缩会话使用的模型**：单独指定用于压缩的模型（默认跟随聊天模型）；
- **朋友圈互动模型**：单独指定用于朋友圈互动（自动发帖/点赞评论）的模型。

### 会话压缩（【我】→ 聊天设置 → 压缩会话）

- 上下文条数滑到【无限制】时自动开启「自动压缩历史消息」；
- 聊天设置页顶部实时显示**当前会话的上下文使用情况**：已用 / 总 token、占用百分比、进度条与压缩阈值刻度线，达到阈值时提示"下次发送将自动压缩"；
- 压缩阈值可手动调整（30% ~ 90%，默认 70%）：当会话历史估算 token 达到所选模型上下文长度的阈值时，自动把更早的历史消息（保留最近 20 条）压缩为一段摘要，并替换进会话记录，避免上下文超长；
- 压缩请求使用「压缩会话使用的模型」完成。

---

## 技术栈

- **Flutter / Dart**：纯 Cupertino 风格，Provider 状态管理
- **数据持久化**：SharedPreferences（设置与数据）
- **依赖**：
  - `http`：LLM 与更新请求
  - `archive` + `gbk_codec`：角色包 zip 解析
  - `image_picker`：图片发送
  - `package_info_plus`：版本号
  - `flutter_local_notifications`：系统通知
  - `flutter_markdown`：Markdown 渲染
  - `gal`：图片保存到相册
  - `image`：图片处理
  - `url_launcher`：打开链接
  - `path_provider`：路径管理
  - `uuid`、`intl`、`lpinyin` 等
